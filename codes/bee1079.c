#include <stdio.h>

int main() {
    long long int x;
    long long int num_one[3], num_two[3], num_thr[3];
    scanf("%lld", &x);
    for (int i = 1; i <= x; i++) {
        scanf("%lld %lld %lld", &num_one[0], &num_one[1], &num_one[2]);
        }
    return 0;
}
