#include <stdio.h>
int main() {
    long long int X, pl, ans, ans_final;
    scanf("%lld", &X);
    long long int i = 1;
    while (i <= X) {
        ans = X % 10;
        i++;
        ans = X / 10;
        }
    printf("%lld", pl);
    return 0;
}
