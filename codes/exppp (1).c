#include <stdio.h>
int main() {
    long long int X, pl, ans = 0;
    scanf("%lld", &X);
    long long int i = 1;
    while (i <= X) {
        if (X != 0) {
        ans = X % 10;
        ans = pl;
        ans = X / 10;
            }
        }
    printf("%lld", pl);
    return 0;
}
